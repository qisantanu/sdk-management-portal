<% team = @team || @team %>
<% context ||= team %>
<% collection ||= :developers %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% with_attribute_settings object: developer do %>
  <tr data-id="<%= developer.id %>">
    <%= render "shared/tables/checkbox", object: developer %>
    <td><%= render 'shared/attributes/text', attribute: :name, url: [:account, developer] %></td>
    <%# 🚅 super scaffolding will insert new fields above this line. %>
    <td><%= render 'shared/attributes/date_and_time', attribute: :created_at %></td>
    <td class="buttons">
      <% unless hide_actions %>
        <% if can? :edit, developer %>
          <%= link_to t('.buttons.shorthand.edit'), [:edit, :account, developer], class: 'button-secondary button-smaller' %>
        <% end %>
        <% if can? :destroy, developer %>
          <%= button_to t('.buttons.shorthand.destroy'), [:account, developer], method: :delete, data: { confirm: t('.buttons.confirmations.destroy', model_locales(developer)) }, class: 'button-secondary button-smaller' %>
        <% end %>
        <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
      <% end %>
    </td>
  </tr>
<% end %>
