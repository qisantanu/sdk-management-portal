<% if can? :read, Developer.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :developers]),
    label: t('developers.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('developers.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
